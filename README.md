# 从代码到实物 - 课程项目日志

这是用于 GitHub Pages 部署的静态站点文件夹。

## 使用方式

1. 将本文件夹内的所有内容上传到 GitHub 仓库根目录。
2. 在仓库 `Settings -> Pages` 中选择部署分支。
3. Source 选择 `Deploy from a branch`，目录选择 `/root`。
4. 访问 GitHub Pages 生成的网址。

入口页面：`index.html`
